# Managed by Ansible — do not edit on the host. Source: group_vars (vault).
POSTGRES_USER={{ postgres_user }}
POSTGRES_PASSWORD={{ postgres_password }}
POSTGRES_DB={{ postgres_db }}

GOVUX_JWT_SECRET={{ govux_jwt_secret }}
GOVUX_SECRET_KEY={{ govux_secret_key }}
GOVUX_CORS_ORIGINS={{ govux_cors_origins }}
GOVUX_CRUX_API_KEY={{ govux_crux_api_key | default('') }}
