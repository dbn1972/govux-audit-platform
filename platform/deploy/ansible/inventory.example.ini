# Copy to inventory.ini and set your host(s).
[govux]
govux-prod-1 ansible_host=10.0.0.10 ansible_user=ubuntu

# For a fleet, add more hosts. Note: this Compose deploy runs a full stack per host;
# for multi-node HA use the Helm chart on Kubernetes instead.

[govux:vars]
ansible_python_interpreter=/usr/bin/python3
